export type Friend = {
    name: string;
    icon: string;
    url: string;
}

export type Article = {
    title: string;
    date: string;
    url: string;
    image?: string;
}

export type RecentPhoto = {
    image: string;
    description: string;
    place: string;
    date: string;
}